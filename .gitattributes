# Prompt 模板强制 LF，避免 Windows 编辑后带入 CRLF。
# PromptTemplateService 运行时也会归一化，这里是第二道防线（提交时就规范）。
*.st text eol=lf

# 其余文本文件按仓库规范存 LF
*.java   text eol=lf
*.py     text eol=lf
*.xml    text eol=lf
*.yaml   text eol=lf
*.yml    text eol=lf
*.sql    text eol=lf
*.md     text eol=lf
*.sh     text eol=lf

# 二进制不做转换
*.parquet binary
*.png     binary
*.jpg     binary
